import 'package:flutter/material.dart';

class ClassDescription extends StatelessWidget {
  const ClassDescription({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}